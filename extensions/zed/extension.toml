id = "tailor"
name = "Tailor"
version = "0.1.0"
schema_version = 1
authors = ["Wess Cope <opensource@wess.io>"]
description = "Build gpui interfaces from Zed's agent panel, through Tailor's document model."
repository = "https://github.com/wess/guise"

# Tailor's MCP server, over the same document the app edits. It saves after
# every change and the app watches the file it has open, so a screen built from
# Zed appears on Tailor's canvas a moment later.
[context_servers.tailor]
